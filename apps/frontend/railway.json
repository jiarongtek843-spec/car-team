{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "npm run build --workspace=apps/frontend"
  },
  "deploy": {
    "startCommand": "npm run start --workspace=apps/frontend",
    "restartPolicyType": "ON_FAILURE"
  }
}

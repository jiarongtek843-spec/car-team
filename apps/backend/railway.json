{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "npm run build --workspace=apps/backend"
  },
  "deploy": {
    "startCommand": "npm run prisma:migrate:deploy --workspace=apps/backend && npm run start --workspace=apps/backend",
    "restartPolicyType": "ON_FAILURE"
  }
}

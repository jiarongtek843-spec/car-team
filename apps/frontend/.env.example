VITE_API_BASE_URL=http://localhost:4000
# Railway staging 部署时改成 Backend service 的 Railway URL，例如：
# VITE_API_BASE_URL=https://backend-staging-xxxx.up.railway.app
# 注意：Vite 的环境变量是打包时(build time)写死进产物的，不是执行时读取——改这个值以后
# 一定要重新 build，改 Railway 环境变量本身不会让已经部署好的 Frontend 生效。
